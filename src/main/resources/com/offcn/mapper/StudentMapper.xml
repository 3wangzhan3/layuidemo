<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.offcn.mapper.StudentMapper">
    <select id="findAll" resultType="student">
        select * from student where 1=1
         <if test="dto.username !=null ">
             and username like #{dto.username}
        </if>
        order by username+0
    </select>

    <insert id="regist" >
        insert into student values(uuid(),#{username},#{password},#{age})
    </insert>

    <select id="getCount" resultType="java.lang.Integer">
        select count(1) from student where 1=1
        <if test="dto.username !=null ">
            and username like #{dto.username}
        </if>
    </select>

    <select id="findUserByName" resultType="student">
        select * from student where username =#{username}
    </select>
</mapper>